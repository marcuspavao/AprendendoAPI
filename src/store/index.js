/* import {createStore} from 'redux';

const INITIAL_STATE = {
    active: null,
    data: [
        'Perfil 1',
        'Perfil 2',
        'Perfil 3'
    ],
}

function perfil(state = INITIAL_STATE, action){
    switch(action.type){
        case 'CHANGE_PROFILE':
            return {...state , active: action.perfil}
        default:
            return state
            
    }
}

export const store = createStore({}); */